<UserControl x:Class="NexusPOS.Shell.Views.StaffAccessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:NexusPOS.Shell.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024"
             Background="{StaticResource BackgroundGrayBrush}">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Border Background="White" CornerRadius="10" Padding="40">
            <StackPanel Width="400">
                <TextBlock Text="Staff Access" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TescoBlueBrush}" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                
                <TextBlock Text="Staff ID" FontWeight="SemiBold"/>
                <TextBox Text="{Binding StaffId}" Padding="10" FontSize="18" Margin="0,5,0,15"/>
                
                <TextBlock Text="PIN Code" FontWeight="SemiBold"/>
                <TextBox Text="{Binding PinCode}" Padding="10" FontSize="18" Margin="0,5,0,15"/> 

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <Button Content="Login" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,10"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButtonStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
