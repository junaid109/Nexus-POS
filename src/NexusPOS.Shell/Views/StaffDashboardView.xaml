<UserControl x:Class="NexusPOS.Shell.Views.StaffDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:NexusPOS.Shell.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024"
             Background="{StaticResource BackgroundGrayBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="{StaticResource TescoBlueBrush}" Padding="20">
            <DockPanel LastChildFill="False">
                <TextBlock Text="Staff Portal" Foreground="White" FontSize="24" FontWeight="Bold" DockPanel.Dock="Left"/>
                <Button Content="Logout" Command="{Binding LogoutCommand}" Style="{StaticResource SecondaryButtonStyle}" Background="Transparent" Foreground="White" BorderBrush="White" FontSize="16" Height="40" Padding="15,5" DockPanel.Dock="Right"/>
            </DockPanel>
        </Border>
        
        <!-- Dashboard Content -->
        <Grid Grid.Row="1" Margin="40">
            <UniformGrid Columns="2" Rows="2">
                <!-- Dashboard Tiles -->
                <Button Margin="10" Style="{StaticResource PrimaryButtonStyle}" Content="Product Lookup"/>
                <Button Margin="10" Style="{StaticResource PrimaryButtonStyle}" Content="Void Transaction"/>
                <Button Margin="10" Style="{StaticResource PrimaryButtonStyle}" Content="System Status"/>
                <Button Margin="10" Style="{StaticResource PrimaryButtonStyle}" Content="Admin Panel (Phase 4)"/>
            </UniformGrid>
        </Grid>
    </Grid>
</UserControl>
