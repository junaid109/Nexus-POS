<UserControl x:Class="NexusPOS.Shell.Views.CustomerLookupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:NexusPOS.Shell.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024"
             Background="{StaticResource BackgroundGrayBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
         <StackPanel Grid.Row="0" Margin="40">
             <TextBlock Text="Search or Key in Item" FontSize="36" FontWeight="Bold" Foreground="{StaticResource TescoBlueBrush}"/>
             <TextBlock Text="Enter Clubcard Number or Product Code" FontSize="18" Foreground="#555555" Margin="0,10,0,0"/>
         </StackPanel>

        <!-- Center Input -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="600">
             <TextBox Text="{Binding CustomerIdentifier}" 
                      Padding="20" 
                      FontSize="32" 
                      Height="80"
                      VerticalContentAlignment="Center"
                      BorderBrush="{StaticResource TescoBlueBrush}"
                      BorderThickness="2"/>
             
             <Button Content="Search" 
                     Command="{Binding SearchCommand}" 
                     Style="{StaticResource PrimaryButtonStyle}" 
                     HorizontalAlignment="Stretch"
                     Margin="0,30,0,0"/>
        </StackPanel>

        <!-- Footer -->
        <Button Grid.Row="2" 
                Content="Go Back" 
                Command="{Binding GoBackCommand}" 
                Style="{StaticResource SecondaryButtonStyle}" 
                HorizontalAlignment="Left"
                Width="200"
                Margin="40,0,0,40"/>

    </Grid>
</UserControl>
