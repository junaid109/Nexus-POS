<UserControl x:Class="NexusPOS.Shell.Views.CustomerOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:NexusPOS.Shell.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024"
             Background="{StaticResource BackgroundGrayBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Welcome back," FontSize="36" Foreground="#555555" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding CustomerName}" FontSize="48" FontWeight="Bold" Foreground="{StaticResource TescoBlueBrush}" HorizontalAlignment="Center" Margin="0,10,0,30"/>
            
            <Border Background="White" CornerRadius="15" Padding="30" BorderBrush="{StaticResource TescoBlueBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Clubcard Points" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                     <TextBlock Text="{Binding PointsBalance}" FontSize="64" FontWeight="Bold" Foreground="{StaticResource TescoRedBrush}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>
        
        <Button Grid.Row="1" Content="Start Shopping" Command="{Binding StartShoppingCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="50" Height="80" FontSize="32"/>

    </Grid>
</UserControl>
